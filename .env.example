MONGO_ROOT_USERNAME=changeme
MONGO_ROOT_PASSWORD=changeme

GRAYLOG_PASSWORD_SECRET=changeme-long-random-string
GRAYLOG_ROOT_PASSWORD_SHA2=sha256-of-your-root-password

INFLUXDB_ADMIN_USERNAME=changeme
INFLUXDB_ADMIN_PASSWORD=changeme
INFLUXDB_ADMIN_TOKEN=changeme

LDAP_ADMIN_PASSWORD=changeme

JWT_SECRET=changeme-long-random-string

GRAFANA_ADMIN_USER=changeme
GRAFANA_ADMIN_PASSWORD=changeme

# Interface reseau de l'hote a ecouter par Suricata (network_mode: host).
# Trouver la bonne interface avec : ip -brief addr show
SURICATA_IFACE=eth0
