global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'chaussec-api'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['chaussec-api:5050']
